<div class="container-fluid text-center">
    <h1>Top Destination</h1>
    <ul class="list-unstyled">
        <% @products.each do |product| %>
        <li><%= link_to product.product_name, product_path(product) %> </li>
        <% end %>
    </ul>
<div class="container-fluid text-center">